name: Build and Deploy to Plesk

on:
  push:
    branches: [main]
    paths-ignore:
      # '*.md' matches ROOT-LEVEL markdown only in GitHub Actions, so a docs-only change
      # in any subfolder still triggered a full deploy. No markdown in this repo is a
      # build input (checked for `import ... .md`, `?raw` and import.meta.glob), so it
      # is safe to ignore markdown at any depth.
      - '*.md'
      - '**/*.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm PRODUCTION deploy'
        required: true
        default: ''

concurrency:
  group: deploy-production
  # Never cancel a mid-flight FTP mirror - a cancelled mirror --delete can leave
  # production in a half-uploaded/half-deleted state (breakage investigation SS6).
  cancel-in-progress: false

jobs:
  validate:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --run

      # Public-safe Supabase client config (publishable key + public project URL -
      # these ship in every client bundle by design; RLS protects data).
      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: https://uyksotlmrlxhmyeopktl.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_PROJECT_ID: uyksotlmrlxhmyeopktl
          # PUBLIC Cloudflare Turnstile site key for the Beize Jass Tour sign-in widget — same
          # class of value as the two above: it ships in every visitor's bundle by design.
          # Without this line the widget in src/components/TurnstileWidget.tsx renders NOTHING
          # (deliberately a no-op when the key is unset) and every sign-in and signup goes out with
          # no captcha token — a TOTAL customer lockout the moment security_captcha_enabled is
          # switched on for uyksotlmrlxhmyeopktl. The step below refuses to let that ship.
          VITE_TURNSTILE_SITE_KEY: ${{ secrets.VITE_TURNSTILE_SITE_KEY }}

      - name: Assert the Turnstile site key actually landed in the bundle (blocking)
        # Runs in `validate` as well as in `deploy` below, so a build that would ship without the
        # captcha widget fails on EVERY push, not only on the manual production dispatch.
        # `vite build` inlines VITE_* at BUILD time, so an empty, missing or misnamed secret
        # produces a bundle with no widget at all — invisible to every runtime monitor, and a total
        # lockout the instant half 2 is flipped. ReplyFlow hit exactly this shape: a dead sitekey
        # behind green monitors. The site key is PUBLIC, so it can be pinned and compared literally.
        run: |
          set -euo pipefail
          expected='0x4AAAAAAEz4uXEnV8i5NHlb'
          if ! grep -rq "$expected" ./dist/; then
            echo "FAIL - the Turnstile site key ($expected) is NOT in the built bundle."
            echo "       vite build inlines VITE_* at build time; the repository secret"
            echo "       VITE_TURNSTILE_SITE_KEY is empty, wrong, or was not passed to the build"
            echo "       step above. Shipping this would render no captcha widget at all, and"
            echo "       every sign-in would break the moment CAPTCHA is enabled on the project."
            exit 1
          fi
          echo "ok - Turnstile site key is present in the bundle ($(grep -rl "$expected" ./dist/ | wc -l) file(s))."

  gate-security:
    if: github.event_name == 'workflow_dispatch'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install gitleaks
        run: |
          curl -sSL https://github.com/gitleaks/gitleaks/releases/download/v8.24.3/gitleaks_8.24.3_linux_x64.tar.gz | tar -xz -C /tmp gitleaks
          chmod +x /tmp/gitleaks

      - name: 'Secret scan: working tree (BLOCKING - new committed-file leaks)'
        run: /tmp/gitleaks dir . --no-banner --redact -v --exit-code 1

      - name: 'Secret scan: full history (AUDIT only, non-blocking - historical keys are public/deprecated)'
        run: /tmp/gitleaks git . --no-banner --redact --log-opts=--all --exit-code 0 || true

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Dependency audit of what ships (blocking at high, production deps only)
        # A RELEASE GATE JUDGES ONLY WHAT SHIPS (Roger, 2026-09-03). `--omit=dev` scopes this to the
        # production dependency tree; the FULL tree, build tooling included, is covered by
        # production-monitor's scheduled fleet dependency audit, which FILES WORK instead of blocking a
        # deploy. Until 2026-09-15 no gate in the fleet carried it, and that was not theoretical: arivioo
        # could not be promoted on 2026-09-14 because of a js-yaml advisory reachable only by running our
        # own linter (eslint -> @eslint/eslintrc -> js-yaml).
        # THE FLOOR IS RESTATED AT THE SAME TIME, because a scope change without one is how a gate quietly
        # becomes decorative. Measured on this repo 2026-09-15, before the change: unscoped at high and
        # scoped at high, so the floor could be raised and the gate got STRICTER on what customers run,
        # not looser. Proven still to refuse: launchready a0b3a12^ (next 16.3.0 + sharp 0.35.3, both in
        # the production tree) exits 1 under this exact command.
        run: npm audit --omit=dev --audit-level=high

      # SAST - code-level security (fleet-hardening Wave 2, proven route from project-starter).
      # A different layer than npm audit (deps) + gitleaks (secrets in git): semgrep reads OUR
      # code for injection / XSS / hardcoded secrets. `.semgrepignore` excludes test/dev files
      # (example tokens + fixtures = false positives). --severity=ERROR = highest confidence;
      # --error fails the gate on a real finding.
      - name: SAST scan (semgrep)
        run: |
          python3 -m pip install --quiet --user --break-system-packages semgrep
          export PATH="$HOME/.local/bin:$PATH"
          semgrep scan --config=p/security-audit --config=p/secrets --severity=ERROR --error --metrics=off .

  deploy:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.confirm == 'deploy'
    needs: [validate, gate-security]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Verify FTP credentials (fail fast on a stale secret)
        # A stale FTP secret used to surface late and confusingly. The site-id guard
        # tests `[ -n "$REMOTE_ID" ]`, and a 530 leaves REMOTE_ID empty - so the guard
        # printed "guard passed (remote='')" at the exact moment auth broke, and the run
        # only failed later inside the mirror, after up to 4 retries. This logs in once,
        # up front, and names the failure. curl exit 67 == FTP login denied; curl needs
        # no lftp, so this can run before checkout. (Added 2026-08-23 after the whole
        # fleet's FTP_PASS went stale at once.)
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          set +e
          # --insecure mirrors `set ssl:verify-certificate no`, which every lftp call in
          # this fleet already uses: the Metanet cert is issued for tertia.sui-inter.net,
          # not for the FTP_HOST value, so verification fails with curl exit 60. The
          # control channel is still TLS; only name verification is skipped.
          # One slow answer from a busy FTP server is not a bad password. Retry the
          # transport, never the credential: curl 67 ("Access denied: 530") is final,
          # because a wrong password does not become right on attempt two. Added after
          # 15 simultaneous fleet deploys made this step time out at 30s (curl 28).
          n=0
          while :; do
            OUT=$(curl -sS --ssl-reqd --insecure --connect-timeout 20 --max-time 90 --user "$FTP_USER:$FTP_PASS" "ftp://$FTP_HOST/" 2>&1)
            RC=$?
            if [ "$RC" -eq 0 ] || [ "$RC" -eq 67 ]; then break; fi
            n=$((n+1))
            if [ "$n" -ge 3 ]; then break; fi
            echo "FTP pre-flight attempt $n failed (curl exit $RC) - retrying in $((n*10))s"
            sleep $((n*10))
          done
          set -e
          if [ "$RC" -eq 67 ]; then
            echo "::error::FTP login denied (530) for $FTP_USER@$FTP_HOST - the FTP password secret is stale. Nothing was deployed."
            exit 1
          fi
          if [ "$RC" -ne 0 ]; then
            echo "::error::FTP connection failed (curl exit $RC) for $FTP_USER@$FTP_HOST. Nothing was deployed."
            printf '%s\n' "$OUT" | head -5
            exit 1
          fi
          echo "FTP auth OK - $(printf '%s\n' "$OUT" | grep -c '') entries at the account root."

      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Database changes ship WITH the release (2026-08-22, fleet drift fix — replyflow
      # pattern): applies every supabase/migrations file not yet in the ledger and fails
      # the deploy on error. Runs BEFORE the site ships so the frontend never goes live
      # ahead of the schema it expects.
      - name: Apply database migrations
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: node scripts/apply-migrations.mjs --project-ref uyksotlmrlxhmyeopktl

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --run

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: https://uyksotlmrlxhmyeopktl.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_PROJECT_ID: uyksotlmrlxhmyeopktl
          # PUBLIC Cloudflare Turnstile site key — see the identical block in `validate` above.
          # This is the build that actually ships, so the assertion after it is the one that
          # stands between a widget-less bundle and the FTP upload.
          VITE_TURNSTILE_SITE_KEY: ${{ secrets.VITE_TURNSTILE_SITE_KEY }}

      - name: Assert the Turnstile site key actually landed in the bundle (blocking)
        # THE ONE THAT GATES THE UPLOAD. Everything below this step puts files on the live host, so
        # a bundle with no captcha widget must die here rather than at the next sign-in.
        run: |
          set -euo pipefail
          expected='0x4AAAAAAEz4uXEnV8i5NHlb'
          if ! grep -rq "$expected" ./dist/; then
            echo "FAIL - the Turnstile site key ($expected) is NOT in the production bundle."
            echo "       vite build inlines VITE_* at build time; the repository secret"
            echo "       VITE_TURNSTILE_SITE_KEY is empty, wrong, or was not passed to the build"
            echo "       step above. Shipping this would render no captcha widget at all, and"
            echo "       every sign-in would break the moment CAPTCHA is enabled on the project."
            exit 1
          fi
          echo "ok - Turnstile site key is present in the production bundle ($(grep -rl "$expected" ./dist/ | wc -l) file(s))."

      - name: Install lftp
        run: sudo apt-get update -qq && sudo apt-get install -y -qq lftp

      - name: Write site-id marker
        run: echo "jass-tour-ui-kit" > dist/.site-id

      - name: Verify deploy target (site-id guard)
        run: |
          REMOTE_ID=$(lftp -u "$FTP_USER,$FTP_PASS" "ftp://$FTP_HOST" -e "set net:timeout 30; set net:max-retries 2; set ftp:ssl-force yes; set ssl:verify-certificate no; set ftp:ssl-protect-data no; cat /Beize-Jass-Tour.mueller.ro/.site-id; quit" 2>/dev/null || echo "")
          # An EMPTY id is not a match failure, so `-n` below let it through - which is exactly
          # what a 530, a timeout or a dropped connection produces. The guard cannot tell those
          # apart from "file absent", so it must not deploy blind. Every live target has a seeded
          # .site-id (all 13 verified reading non-empty on 2026-08-23); a genuinely new target must
          # have its .site-id seeded first. Copied in spirit from Valrano, which had this right.
          if [ -z "$REMOTE_ID" ]; then
            echo "::error::Site-id guard INCONCLUSIVE: read no .site-id (timeout, dropped connection, bad credential or missing file - indistinguishable). Refusing to deploy blind."
            exit 1
          fi
          if [ -n "$REMOTE_ID" ] && [ "$REMOTE_ID" != "jass-tour-ui-kit" ]; then
            echo "::error::DEPLOY ABORTED - remote .site-id is '$REMOTE_ID', expected 'jass-tour-ui-kit'. Wrong FTP target!"
            exit 1
          fi
          echo "Site-id guard passed (remote='${REMOTE_ID:-<none>}')"
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}

      - name: Deploy via FTP
        run: |
          # set +e: GitHub runs steps with bash -e, which aborts OUT=$(lftp...) on the
          # first failure BEFORE this retry loop can iterate (dead-retry, deploy-standard SS3f).
          set +e
          n=0
          until [ $n -ge 4 ]; do
          OUT=$(lftp -u "$FTP_USER,$FTP_PASS" "ftp://$FTP_HOST" <<SCRIPT 2>&1
          set ftp:ssl-force yes; set ssl:verify-certificate no; set ftp:ssl-protect-data no
          set ftp:passive-mode yes; set ftp:prefer-epsv no; set net:timeout 30; set net:max-retries 4; set net:persist-retries 4; set net:reconnect-interval-base 8; set mirror:parallel-transfer-count 4
          mirror --reverse --verbose --no-perms \
            ./dist/ /Beize-Jass-Tour.mueller.ro/
          bye
          SCRIPT
          )
          RC=$?
          echo "$OUT"
          [ $RC -eq 0 ] && { echo "Deploy ok (attempt $((n+1)))"; break; }
          # 530 is an FTP REPLY CODE, so it only counts where a reply code can actually appear: at
          # the start of a line, or straight after lftp's "Access failed:", and followed by
          # whitespace + a word. A bare '530' substring matched a hashed ASSET FILENAME on
          # 2026-09-02 (Inter_100Thin_Italic.1b97f7df9b976cfe530c18c09598e6f6.ttf) and killed a
          # perfectly healthy deploy with "stale secret, not retrying" - the one verdict that
          # deliberately does NOT retry. Shared test: gate-kit test/ftp-reply-code-guard.test.mjs.
          echo "$OUT" | grep -qiE '(^|[^[:alnum:]])530[[:space:]]+[A-Za-z]|login (incorrect|failed)' && { echo "::error::FTP auth failed (530) - stale FTP secret, not transient; not retrying."; exit 1; }
          n=$((n+1)); [ $n -ge 4 ] && { echo "::error::Deploy failed after 4 attempts"; exit 1; }
          echo "::warning::attempt $n failed (transient); retrying in 25s..."; sleep 25
          done
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}

      - name: Prune stale assets (14-day retention)
        # Age-based cleanup of the hashed chunks the deploy leaves behind. This
        # repo's mirror has no --delete, so old hashed chunks are never deleted at
        # deploy time (open tabs never 404 on them) — but they accumulate on the
        # server until pruned here. lftp has no find -mtime (only -max-depth/-ls —
        # verified against the 4.8.1 man page), so this probes each file's mtime
        # over raw FTP MDTM (RFC 3659, supported by the FTP server on this host)
        # and deletes files older than 14 days. 14 days >> any realistically open
        # tab, so stale-tab chunk fetches 404 only for sessions older than the
        # retention window.
        #
        # Never fails the deploy: a cleanup hiccup must not block promotion-relevant
        # runs; it logs ::error:: (red annotation, run still green) and the next deploy
        # retries. Capped at 1000 deletions per run so a logic bug cannot mass-delete
        # and a long outage self-heals progressively over successive deploys.
        continue-on-error: true
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          ASSETS_DIR: /Beize-Jass-Tour.mueller.ro/assets
        run: |
          # GitHub wraps every run block in bash -e: a failing command substitution in an
          # assignment (e.g. LIST=$(lftp ...)) would abort the whole step BEFORE the script's
          # own rc guards run. Cleanup must never abort mid-run — disable -e first.
          set +e
          set -u
          export TZ=UTC
          DAYS=14
          CUTOFF=$(date -u -d "$DAYS days ago" +%Y%m%d%H%M%S)
          SETTINGS="set ftp:ssl-force yes
          set ssl:verify-certificate no; set ftp:ssl-protect-data no
          set ftp:passive-mode yes; set net:timeout 60"

          LIST=$(timeout 180 lftp -u "$FTP_USER,$FTP_PASS" "ftp://$FTP_HOST" <<SCRIPT 2>&1
          $SETTINGS
          find $ASSETS_DIR
          quit
          SCRIPT
          )
          if [ $? -ne 0 ]; then
            echo "::error::Asset prune: listing $ASSETS_DIR failed - leaving cleanup to the next deploy. Output: $(echo "$LIST" | head -3)"
            exit 0
          fi
          if [ -z "$(echo "$LIST" | tr -d '[:space:]')" ]; then
            echo "Asset prune: $ASSETS_DIR is empty - nothing to do."
            exit 0
          fi

          # Probe every file's mtime in ONE lftp session: echo a PROBE: name line before
          # each quote MDTM so reply lines stay paired with their file even when a file
          # vanishes (550) between listing and probing.
          PROBE_SCRIPT=$(mktemp)
          {
            echo "open -u $FTP_USER,$FTP_PASS ftp://$FTP_HOST"
            echo "$SETTINGS"
            # find prints full remote paths ("$ASSETS_DIR/<nested>/file", recursively —
            # nested chunk dirs are covered too); normalize to a path relative to
            # ASSETS_DIR so either output format is safe.
            echo "$LIST" | while IFS= read -r p; do
              p="${p%$'\r'}"
              [ "$p" = "$ASSETS_DIR" ] && continue
              rel="${p#"$ASSETS_DIR"/}"
              # Hashed asset names only ([A-Za-z0-9._/-], no spaces/globs) — anything
              # else is skipped, never deleted.
              [[ "$rel" =~ ^[A-Za-z0-9._/-]+$ ]] || continue
              echo "echo \"PROBE:$ASSETS_DIR/$rel\""
              echo "quote MDTM $ASSETS_DIR/$rel"
            done
            echo "quit"
          } > "$PROBE_SCRIPT"

          OUT=$(timeout 420 lftp -f "$PROBE_SCRIPT" 2>&1)
          rm -f "$PROBE_SCRIPT"

          # Pair each PROBE: line with the following 213/550 reply; 14-digit UTC
          # timestamps compare correctly as plain strings against the 14-digit cutoff.
          PAIRED=$(awk '
            /^PROBE:/ { file=substr($0, 7); next }
            file != "" {
              if ($1 == "213" && $2 ~ /^[0-9]{14}$/) print $2, file
              file=""
            }' <<< "$OUT")
          if [ -z "$PAIRED" ]; then
            echo "::error::Asset prune: no MDTM replies from $ASSETS_DIR (server may not support RFC 3659 MDTM) - leaving cleanup to the next deploy."
            exit 0
          fi

          OLD=$(sort -n <<< "$PAIRED" | awk -v cutoff="$CUTOFF" '$1 < cutoff {print $2}' | head -1000)
          COUNT=$(echo "$OLD" | grep -c . || true)
          if [ "$COUNT" -eq 0 ]; then echo "Asset prune: nothing older than $DAYS days in $ASSETS_DIR."; exit 0; fi

          DEL_SCRIPT=$(mktemp)
          {
            echo "open -u $FTP_USER,$FTP_PASS ftp://$FTP_HOST"
            echo "$SETTINGS"
            # lftp rm does NOT glob-expand - hashed names are passed verbatim.
            # $OLD already holds full remote paths (the PROBE: values).
            echo "$OLD" | while IFS= read -r f; do echo "rm $f"; done
            echo "quit"
          } > "$DEL_SCRIPT"
          DEL_OUT=$(timeout 300 lftp -f "$DEL_SCRIPT" 2>&1)
          rm -f "$DEL_SCRIPT"
          echo "$DEL_OUT"
          echo "Asset prune: deleted $COUNT file(s) older than $DAYS days from $ASSETS_DIR."
          exit 0
